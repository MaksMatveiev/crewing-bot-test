# Крюинг-бот: зависимости

python-dotenv>=1.0     # чтение .env
openai>=1.40           # клиент к Gemini (OpenAI-совместимый API)
gradio>=6.0            # веб-интерфейс: чат кандидата и вкладка рекрутера
psycopg2-binary>=2.9   # Postgres (Supabase)
pytest>=8.0            # тесты

# Приезжали транзитивно через gradio, но раз app.py импортирует их напрямую
# для вебхука Telegram — указываем явно.
fastapi>=0.141         # веб-сервер для приёма вебхука Telegram
uvicorn>=0.52          # ASGI-сервер, на котором поднимается приложение
